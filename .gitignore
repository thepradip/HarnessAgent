# Python
__pycache__/
*.py[cod]
*.pyo
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.egg-info/
dist/
build/
htmlcov/
coverage.xml
.coverage

# Virtual environments
.venv/
venv/
env/

# Environment secrets
.env
.env.local
.env.*.local

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Project runtime — local only, never pushed
/workspaces/
/logs/
/runs/
*.db
*.sqlite
*.sqlite3
memory_service.db

# Poetry
poetry.lock

# Docker volumes (generated)
data/

# MLflow artifacts — local only
mlruns/
mlflow.db
artifacts/

# Log files — local only
*.log
*.jsonl
logs/
run_logs/

# Models (large binary files)
models/*.gguf
models/*.bin
models/*.safetensors

# Downloaded browser binaries and generated output artifacts
/chrome/
/output/

# PDFs and pitch decks — local only
# NOTE: /docs/ is the published GitHub Pages site (Settings → Pages → main /docs); do NOT ignore it
*.pdf
*.pptx
*.ppt

# Paper draft — local only, not pushed
/paper/

# Planning / design notes — local only, never pushed
*_PLAN.md
GAP_PLAN.md

# Benchmark cache (large SQLite DBs + intermediate results)
benchmarks/cache/

# Large dataset files
benchmarks/results/gsm8k_test.jsonl
